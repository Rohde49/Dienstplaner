import type { CalculationType } from '../../../shared/schemas';

export const CALCULATION_TYPE_LABELS: Record<CalculationType, string> = {
  fixed: 'Feste Zeitwerte',
  weeklyWorkingTime: 'Wochenarbeitszeit',
};
